# Workspace

Drop PDFs into `inbox/`, then run `poetry run python scripts/run_from_pdf.py` (requires `langgraph dev --no-browser` running).
Results appear in `output/<pdf-name>/` as `results.json` and `report.md`.
